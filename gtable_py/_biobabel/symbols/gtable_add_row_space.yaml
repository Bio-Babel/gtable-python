# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for gtable_py.gtable_add_row_space.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: gtable_py.gtable_add_row_space
import_path: gtable_py.gtable_add_row_space
kind: function
signature: 'gtable_add_row_space(x: ''Gtable'', height: ''Unit'') -> ''Gtable'''
purpose: Add spacing between rows.
description: Add spacing between rows.
parameters:
- name: x
  type: Gtable
  required: true
  default: null
- name: height
  type: Unit
  required: true
  default: null
mutates: ''
returns: Gtable
requires: []
writes: []
examples:
- 'tab = gtable_add_row_space(tab, Unit(5, "mm"))  # uniform gap between every adjacent row pair'
- 'tab = gtable_add_row_space(tab, Unit([8, 3], "mm"))  # per-pair gaps; length must be nrow-1'
failure_fixes:
- when: 'ValueError: `height` must be of length 1 or nrow - 1'
  suggest:
  - 'Pass a length-1 Unit (one gap, recycled) or a length-(nrow-1) Unit (one gap per adjacent pair).'
  explanation: 'A table with 0 or 1 rows has no inter-row gaps, so the call returns the table unchanged.'
related:
- gtable_py.gtable_add_col_space
- gtable_py.gtable_add_rows
