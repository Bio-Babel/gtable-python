# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for gtable_py.gtable_width.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: gtable_py.gtable_width
import_path: gtable_py.gtable_width
kind: function
signature: 'gtable_width(x: ''Gtable'') -> ''Unit'''
purpose: Return the total width of a gtable.
description: Return the total width of a gtable.
parameters:
- name: x
  type: Gtable
  required: true
  default: null
mutates: ''
returns: Unit
requires: []
writes: []
examples:
- 'gtable_width(tab)  # Unit = sum of tab.widths (Unit(0, "cm") when there are no columns)'
failure_fixes:
- when: 'gtable_width / gtable_height (or convert_width on it) gives 0 or a null Unit'
  suggest:
  - 'The table has "null" (flexible) tracks; their total width is itself a null Unit with no absolute size. To get resolved physical sizes, rebuild GridLayout(nrow=x.nrow, ncol=x.ncol, widths=x.widths, heights=x.heights) and query layout_region per track (grid_py idiom resolve_null_track_mm).'
  explanation: 'gtable_width sums the width Units; a sum that includes null units stays null and converts to 0.'
related:
- gtable_py.gtable_height
