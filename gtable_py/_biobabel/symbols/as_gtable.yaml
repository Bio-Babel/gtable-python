# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for gtable_py.as_gtable.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: gtable_py.as_gtable
import_path: gtable_py.as_gtable
kind: function
signature: 'as_gtable(x: ''Any'', *, widths: ''Optional[Unit]'' = None, heights: ''Optional[Unit]''
  = None) -> ''Gtable'''
purpose: Convert an object to a Gtable.
description: Convert an object to a Gtable.
parameters:
- name: x
  type: Any
  required: true
  default: null
- name: widths
  type: Optional[Unit]
  required: false
  default: null
- name: heights
  type: Optional[Unit]
  required: false
  default: null
mutates: ''
returns: Gtable
requires: []
writes: []
examples:
- 'as_gtable(rect_grob())  # wrap one grob in a 1x1 Gtable (size taken from grobWidth/grobHeight)'
- 'as_gtable(tab)  # already a Gtable -> returned unchanged'
failure_fixes:
- when: 'TypeError: Cannot convert <type> to a Gtable.'
  suggest:
  - 'Pass a Gtable (returned as-is) or a grid_py grob (wrapped in a 1x1 table).'
  - 'To assemble multiple cells, use Gtable(...) + gtable_add_grob or gtable_matrix instead.'
related:
- gtable_py.Gtable
- gtable_py.is_gtable
