# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for gtable_py.cbind_gtable.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: gtable_py.cbind_gtable
import_path: gtable_py.cbind_gtable
kind: function
signature: 'cbind_gtable(*args: ''Gtable'', size: ''str'' = ''max'', z: ''Optional[List[float]]''
  = None) -> ''Gtable'''
purpose: Column-bind multiple gtables.
description: Column-bind multiple gtables.
parameters:
- name: args
  type: Gtable
  required: true
  default: null
- name: size
  type: str
  required: false
  default: max
- name: z
  type: Optional[List[float]]
  required: false
  default: null
mutates: ''
returns: Gtable
requires: []
writes: []
examples:
- 'cbind_gtable(left, right)  # stack horizontally; inputs must share nrow'
- 'cbind_gtable(a, b, size="max")  # reconcile row heights to the larger of each pair'
failure_fixes:
- when: 'ValueError: `x` and `y` must have the same number of rows'
  suggest:
  - 'Every table passed to cbind_gtable must have the same row COUNT (nrow).'
  - 'size="first"/"last"/"min"/"max" only reconciles differing row HEIGHTS, not counts.'
  explanation: 'cbind stacks tables horizontally, so they must align row-for-row; pad a shorter table with gtable_add_rows first.'
related:
- gtable_py.rbind_gtable
